<?php

use Illuminate\Database\Seeder;
use {model_namespaced};

class {name} extends Seeder {

	public function run()
	{
		foreach($this->getData() as $data)
		{
			{model}::create($data);
		}
	}

	protected function getData()
	{
		return [
			[],
		];
	}
}