<?php namespace {namespace};

use Melon\Database\Eloquent\EloquentMelonRepository;

class {name}
{
	use EloquentMelonRepository;

	/**
     * @param {model} $model
     */
    function __construct({model} $model)
    {
        $this->model = $model;
    }
}